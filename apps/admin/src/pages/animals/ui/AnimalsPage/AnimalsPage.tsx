import { JSX } from 'react';
import { SectionPlaceholder } from '../../../../shared/ui';

export const AnimalsPage = (): JSX.Element => {
	return <SectionPlaceholder title="Животные" />;
};

export default AnimalsPage;
