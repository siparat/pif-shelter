import { JSX } from 'react';
import { SectionPlaceholder } from '../../../../shared/ui';

export const MeetingsPage = (): JSX.Element => {
	return <SectionPlaceholder title="Заявки на встречу" />;
};

export default MeetingsPage;
