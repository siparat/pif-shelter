import { JSX } from 'react';

export const Header = (): JSX.Element => {
	return (
		<header className="h-20 bg-white/60 backdrop-blur-xl sticky top-0 border-b border-[#e6e0da] px-10 flex items-center justify-between z-50"></header>
	);
};
